import { Meta, ColorPalette, ColorItem } from "@storybook/blocks";
import { defaultTheme } from "@react-ck/theme";

<Meta title="Providers/Theme/Palette" />

# Theme Palette

Explore the color palette used in our default theme.
These colors are carefully curated to maintain a cohesive and visually appealing user interface across various components in your application.

<ColorPalette>
  <ColorItem
    title="Highlight Colors"
    subtitle="Mostly used for CTA's"
    colors={Object.fromEntries(
      Object.entries(defaultTheme.color).filter(([k]) => k.includes("highlight")),
    )}
  />

  <ColorItem
    title="Neutral Light"
    subtitle="Multi purpose neutral light colors"
    colors={Object.fromEntries(
      Object.entries(defaultTheme.color).filter(([k]) => k.includes("neutral-light")),
    )}
  />

  <ColorItem
    title="Neutral Dark"
    subtitle="Multi purpose neutral dark colors"
    colors={Object.fromEntries(
      Object.entries(defaultTheme.color).filter(([k]) => k.includes("neutral-dark")),
    )}
  />

  <ColorItem
    title="Status Colors"
    subtitle="Colors used to refelect different statuses"
    colors={Object.fromEntries(
      Object.entries(defaultTheme.color).filter(([k]) => k.includes("status")),
    )}
  />

  <ColorItem
    title="Overlay"
    subtitle="Semi-opaque colors"
    colors={Object.fromEntries(
      Object.entries(defaultTheme.color).filter(([k]) => k.includes("overlay")),
    )}
  />
</ColorPalette>
