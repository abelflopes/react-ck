export interface Theme {
  color: {
    highlightPrimary: string;
    highlightSecondary: string;
    text: string;
    textAlt: string;
    neutral100: string;
    neutral200: string;
  };
  font: {
    family: string;
    baseSize: string;
  };
  spacing: {
    base: string;
  };
}
